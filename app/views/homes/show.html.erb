<p><%= @home.name %>|| <%= @home.location %> <%= @home.eco_function %> <%= @home.home_away_function %> </p>


<%# <%= link_to "Delete", home_path(@home), method: :delete, data: { confirm: "Are you sure?" } %>
<%# <%= link_to "edit", home_path(@home), method: :edit, data: { confirm: "Are you sure?" } %>

<% @devices.each do |device| %>
 <p><%= device.name %></p>
 <p><%= device.category %></p>
    <% device.energies.each do |energy| %>
      <%= energy.kilowatts %>
    <% end %>
<% end %>
<br>
<br>
<%= simple_form_for [@home, @device] do |f| %>
  <%= f.input :name %>
  <%= f.input :category %>
  <%= f.button :submit %>
<% end %>




<br>
<%= link_to "Return to Homes", homes_path %>

<br>
<br>

<%= link_to "Edit Home", edit_home_path %>