<div class="page-padding">
<h1 class="name-home-show"><%= @home.name %></h1>
<h3 class="location-home-show"> <%= @home.location %> </h3>
 <p>Eco-State:<%= @home.eco_function %></p>
 <p>Home-Away:<%= @home.home_away_function %>


<%# <%= link_to "Delete", home_path(@home), method: :delete, data: { confirm: "Are you sure?" } %>
<%# <%= link_to "edit", home_path(@home), method: :edit, data: { confirm: "Are you sure?" } %>

<h4> Your Devices </h4>

<% @devices.each do |device| %>
 <p><%= device.name %></p>
 <p><%= device.category %></p>
    <% device.energies.each do |energy| %>
      <%= energy.kilowatts %>
    <% end %>
<% end %>

<button class="btn-home"><%= link_to "Return to Homes", homes_path, class: "btn-home-text"%>
</button>
